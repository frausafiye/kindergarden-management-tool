import SingleGroupEdit from "../../features/groupFeatures/editGroup/SingleGroupEdit";
import { AlignedContainer } from "../../components/ui/styledComponents";

export default function SingleGroupEditPage(props) {
  return (
    <AlignedContainer>
      <SingleGroupEdit />
    </AlignedContainer>
  );
}
