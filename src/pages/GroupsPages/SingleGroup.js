import SingleGroup from "../../features/groupFeatures/displayGroups/SingleGroup";
import { AlignedContainer } from "../../components/ui/AlignedContainer";

export default function SingleGroupPage() {
  return (
    <AlignedContainer>
      <SingleGroup />
    </AlignedContainer>
  );
}
