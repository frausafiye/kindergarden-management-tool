import { AlignedContainer } from "../../components/ui/AlignedContainer";
import AllGroups from "../../features/groupFeatures/displayGroups/AllGroups";

export default function AllGroupsPage(props) {
  return (
    <AlignedContainer>
      <AllGroups />
    </AlignedContainer>
  );
}
